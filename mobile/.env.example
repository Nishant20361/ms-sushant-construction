# Public configuration only. Never put server secrets in EXPO_PUBLIC_* values.
# Physical device: replace MAC_LAN_IP and keep phone/Mac on the same network.
EXPO_PUBLIC_API_BASE_URL=http://MAC_LAN_IP:5100/api/public

# Production:
# EXPO_PUBLIC_API_BASE_URL=https://ms-sushant-construction.onrender.com/api/public
