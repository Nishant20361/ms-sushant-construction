import { Pressable, StyleSheet, Text, TextInput, View } from "react-native";
import { theme } from "@/theme";
export function AssistantComposer({ value, sending, onChange, onSend }: { value: string; sending: boolean; onChange(value: string): void; onSend(): void }) { const disabled = sending || !value.trim(); return <View style={styles.container}><TextInput accessibilityLabel="Ask Construction Assistant" value={value} onChangeText={onChange} editable={!sending} multiline maxLength={2000} placeholder="Ask in Hindi, English or Hinglish…" placeholderTextColor={theme.colors.muted} textAlignVertical="top" style={styles.input} /><Pressable accessibilityRole="button" accessibilityLabel="Send message" accessibilityState={{ disabled, busy: sending }} disabled={disabled} onPress={onSend} style={[styles.send, disabled && styles.disabled]}><Text style={styles.sendText}>Send</Text></Pressable></View>; }
const styles = StyleSheet.create({ container: { flexDirection: "row", alignItems: "flex-end", gap: 9, paddingHorizontal: 12, paddingTop: 10, paddingBottom: 8, borderTopWidth: 1, borderTopColor: theme.colors.border, backgroundColor: theme.colors.surface }, input: { flex: 1, minHeight: 48, maxHeight: 120, paddingHorizontal: 13, paddingVertical: 11, borderWidth: 1, borderColor: theme.colors.border, borderRadius: 15, backgroundColor: theme.colors.background, color: theme.colors.text }, send: { minWidth: 64, minHeight: 48, alignItems: "center", justifyContent: "center", borderRadius: 14, backgroundColor: theme.colors.primary }, disabled: { opacity: .45 }, sendText: { color: "white", fontWeight: "900" } });
