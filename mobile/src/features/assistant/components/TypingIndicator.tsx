import { ActivityIndicator, StyleSheet, Text, View } from "react-native";
import { theme } from "@/theme";
export function TypingIndicator({ onCancel }: { onCancel(): void }) { return <View accessibilityRole="progressbar" accessibilityLabel="Construction Assistant is thinking" style={styles.row}><View style={styles.bubble}><ActivityIndicator size="small" color={theme.colors.primary} /><Text style={styles.text}>Thinking…</Text><Text style={styles.calm}>The server may take a moment to start.</Text><Text accessibilityRole="button" accessibilityLabel="Cancel assistant request" onPress={onCancel} style={styles.cancel}>Cancel</Text></View></View>; }
const styles = StyleSheet.create({ row: { paddingHorizontal: 14, paddingVertical: 5, alignItems: "flex-start" }, bubble: { minWidth: 180, padding: 13, borderWidth: 1, borderColor: theme.colors.border, borderRadius: 16, borderBottomLeftRadius: 5, backgroundColor: theme.colors.surface }, text: { marginTop: 7, fontWeight: "800", color: theme.colors.text }, calm: { marginTop: 3, fontSize: 12, color: theme.colors.muted }, cancel: { minHeight: 44, paddingTop: 11, fontWeight: "800", color: theme.colors.primary } });
