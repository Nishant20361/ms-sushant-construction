import { Tabs } from "expo-router";
import { Ionicons } from "@expo/vector-icons";
import { useSafeAreaInsets } from "react-native-safe-area-context";
import { useTheme } from "@/theme";
const tabs = [["index", "Dashboard", "grid", "grid-outline"], ["orders", "Orders", "receipt", "receipt-outline"], ["products", "Products", "cube", "cube-outline"], ["dues", "Dues", "wallet", "wallet-outline"], ["more", "More", "menu", "menu-outline"]] as const;
export default function TabsLayout() { const t = useTheme(); const insets = useSafeAreaInsets(); return <Tabs screenOptions={{ headerShown: false, tabBarActiveTintColor: t.colors.brand, tabBarInactiveTintColor: t.colors.textMuted, tabBarActiveBackgroundColor: t.colors.brandSoft, tabBarStyle: { height: 58 + insets.bottom, paddingBottom: Math.max(insets.bottom, 6), paddingTop: 5, backgroundColor: t.colors.surfaceElevated, borderTopColor: t.colors.border, elevation: 5, shadowColor: t.colors.shadow, shadowOpacity: t.resolved === "dark" ? 0 : .07, shadowRadius: 10 }, tabBarLabelStyle: { fontSize: 11, lineHeight: 15, fontWeight: "700" }, tabBarItemStyle: { minHeight: 48, borderRadius: t.radius.medium, marginHorizontal: 2 }, sceneStyle: { backgroundColor: t.colors.background } }}>{tabs.map(([name, title, active, inactive]) => <Tabs.Screen key={name} name={name} options={{ title, tabBarAccessibilityLabel: `${title} tab`, tabBarIcon: ({ focused, color, size }) => <Ionicons name={focused ? active : inactive} color={color} size={Math.min(size, 23)} /> }} />)}</Tabs>; }
