import {describe,expect,it} from "vitest";import {dateError,discountError,dueQuery,finalPreview,normalizeBill,normalizeCustomer,normalizeCustomerDetail,paymentError,query} from "./helpers";
describe("due normalization",()=>{it("normalizes customer numeric strings",()=>{expect(normalizeCustomer({customerName:"A",totalDue:"125.5",orders:null})).toMatchObject({customerName:"A",totalDue:125.5,orders:[]})});it("normalizes payment status and history",()=>{const d=normalizeCustomerDetail({orders:[{paymentStatus:"FUTURE"}],paymentHistory:[{amount:"10",paymentMode:"CASH",previousDue:"20",remainingDue:"10"}]});expect(d.orders[0].paymentStatus).toBe("UNKNOWN");expect(d.paymentHistory[0]).toMatchObject({amount:10,paymentMode:"CASH",previousDue:20,remainingDue:10})})});
describe("payment validation",()=>{it("rejects non-positive and overpayment",()=>{expect(paymentError("0",100)).toContain("greater than zero");expect(paymentError("101",100)).toContain("exceed");expect(paymentError("50",100)).toBeNull()});it("accepts only supported date form",()=>{expect(dateError("21/08/2026")).toContain("YYYY-MM-DD");expect(dateError("2026-08-21")).toBeNull()});it("serializes exact due filters",()=>{expect(dueQuery({search:"A",dateRange:"week",paymentStatus:"DUE",from:"",to:""})).toContain("paymentStatus=DUE")})});
describe("billing",()=>{it("normalizes nullable bills",()=>{expect(normalizeBill(null)).toBeNull();expect(normalizeBill({id:"1",discount:"5",finalAmount:"95"})).toMatchObject({id:1,discount:5,finalAmount:95})});it("validates discount and previews final",()=>{expect(discountError("101",100)).toContain("exceed");expect(discountError("10",100)).toBeNull();expect(finalPreview(100,"10.25")).toBe(89.75)});it("serializes payment payload fields",()=>{expect(query({amount:50,paymentMode:"ONLINE",paymentDate:"2026-08-21"})).toBe("amount=50&paymentMode=ONLINE&paymentDate=2026-08-21")})});
