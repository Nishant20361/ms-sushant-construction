import { describe,expect,it } from "vitest";
import { normalizeOrder,normalizeOrdersPage,normalizeProduct } from "./normalize";
import { mergeUniqueOrders,needsWhole,serializeOrderQuery,validDateRange } from "./helpers";
describe("order normalization",()=>{it("normalizes numeric strings and unknown enums",()=>{expect(normalizeOrder({id:"4",finalAmount:"12.5",status:"FUTURE",paymentStatus:null,items:null})).toMatchObject({id:4,finalAmount:12.5,status:"UNKNOWN",paymentStatus:"UNKNOWN",items:[]})});it("bounds page metadata",()=>{expect(normalizeOrdersPage({page:0,pages:0,orders:"bad"})).toMatchObject({page:1,pages:1,orders:[]})});it("normalizes picker products",()=>{expect(normalizeProduct({id:"2",stock:"1.25",isActive:true})).toMatchObject({id:2,stock:1.25,isActive:true})})});
describe("order validation",()=>{it("validates ISO ranges",()=>{expect(validDateRange("2026-08-22","2026-08-21")).toContain("after");expect(validDateRange("21/08/2026","")).toContain("YYYY-MM-DD");expect(validDateRange("2026-08-20","2026-08-21")).toBeNull()});it("restricts bag and piece quantities",()=>{expect(needsWhole("Bag")).toBe(true);expect(needsWhole("kg")).toBe(false)})});
describe("order queries",()=>{it("uses the backend paymentStatus and date names",()=>{const result=serializeOrderQuery({paymentStatus:"DUE",from:"2026-08-01",to:"2026-08-21",page:2});expect(result).toContain("paymentStatus=DUE");expect(result).not.toContain("payment=");expect(result).toContain("from=2026-08-01")});it("merges pagination without duplicate ids",()=>{expect(mergeUniqueOrders([[{id:1},{id:2}],[{id:2},{id:3}]])).toEqual([{id:1},{id:2},{id:3}])})});
